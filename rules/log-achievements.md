# Log Achievements

When a significant milestone completes — PR created/merged (`gh pr create`, `gh pr merge`), design doc finalized, important ad-hoc task done — offer once:

> "Want me to log this as an achievement? `/log-achievement`"

Skip routine, small, or repetitive tasks.
