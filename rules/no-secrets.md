# No Secrets Access

Never prompt, ask to read, or access tokens, API keys, passwords, credentials, or secrets — including from env vars, `.env`, config files, or secret managers.

If a task requires credentials, tell user what is needed; let them provide it.
